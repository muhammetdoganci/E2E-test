package tests.calismalar;
/*

import com.github.javafaker.Faker;
import org.openqa.selenium.Keys;
import org.openqa.selenium.interactions.Actions;
import org.testng.annotations.Test;
import org.testng.asserts.SoftAssert;
import pages.AutoPage;
import utilities.ConfigReader;
import utilities.Driver;

public class Soru1 {
    @Test
    public void test1(){
        /*

        // 1. Tarayıcıyı başlat
        // 2. 'http://automationexercise.com' url'sine gidin
        Driver.getDriver().get(ConfigReader.getProperty("autUrl"));
        Faker faker = new Faker();
        AutoPage autoPage = new AutoPage();
        SoftAssert softAssert = new SoftAssert();
        // 3. Ana sayfanın başarıyla görünür olduğunu doğrulayın
        softAssert.assertTrue(autoPage.anasayfa.isDisplayed());
        // 4. 'Sepet' düğmesine tıklayın
        autoPage.sepet.click();
        // 5. Altbilgiye ilerleyin
        Actions actions =new Actions(Driver.getDriver());
        actions.sendKeys(Keys.PAGE_DOWN).perform();
        // 6. 'ABONELİK' metnini doğrulayın
        softAssert.assertTrue(autoPage.abonelik.isDisplayed());
        // 7. Girişte e-posta adresini girin ve ok düğmesine tıklayın
        autoPage.email.sendKeys(ConfigReader.getProperty("autuEmailGiris"),Keys.ENTER);
        // 8. Başarı mesajını doğrulayın 'Başarıyla abone oldunuz!' görünür
        softAssert.assertTrue(autoPage.girisBasarili.isDisplayed());

    }


}

 */

